using UnityEngine;

/// <summary>
/// Audio reaction.
/// Reaction used to Play Sounds through a given AudioSource.
/// The AudioSource handles delay, this is a Reaction rather than a DelayedReaction.
/// </summary>

public class AudioReaction : Reaction {
	//
}
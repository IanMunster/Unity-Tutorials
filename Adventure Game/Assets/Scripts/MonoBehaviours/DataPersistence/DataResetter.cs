using UnityEngine;

/// <summary>
/// Data resetter.
/// Use to Reset Scriptable Objecs. Usefull in Editor when SerializedDate can Persist. (between entering and exiting play mode)
/// It is also useful for situations where the game needs to reset without being closed, for example a new play through.
/// </summary>

public class DataResetter : MonoBehaviour {
	//
}
